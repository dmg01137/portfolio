<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.dao.log.PatternLogDAO">

    <!-- 패턴 로그에 대한 결과 매핑 -->
    <resultMap id="patternLogResultMap"
                type="com.example.demo.dto.log.PatternLog">
        <id property="id" column="id" />
        <result property="time" column="time" />
        <result property="sIp" column="s_ip" />
        <result property="dIp" column="d_ip" />
        <result property="sPort" column="s_port" />
        <result property="dPort" column="d_port" />
        <result property="len" column="len" />
        <result property="pattern1" column="pattern1" />
        <result property="pattern2" column="pattern2" />
        <result property="pattern3" column="pattern3" />
        <result property="packet" column="packet" />
        <result property="actionType" column="action_type" />
    </resultMap>

    <!-- ID를 기반으로 패턴 로그를 조회합니다 -->
    <select id="findById" resultMap="patternLogResultMap"
            parameterType="int">
        SELECT * FROM log_db.pattern_log_20240719 WHERE id = #{id}
    </select>

    <!-- 모든 패턴 로그를 조회합니다 -->
    <select id="findAll" resultMap="patternLogResultMap">
        SELECT * FROM log_db.pattern_log_20240719
    </select>

    <!-- 새로운 패턴 로그를 저장합니다 -->
    <insert id="save"
            parameterType="com.example.demo.dto.log.PatternLog">
        INSERT INTO log_db.pattern_log_20240719 (time, s_ip, d_ip, s_port, d_port, len,
                                        pattern1, pattern2, pattern3, packet, action_type)
        VALUES (#{time}, #{sIp}, #{dIp}, #{sPort}, #{dPort}, #{len}, #{pattern1},
                #{pattern2}, #{pattern3}, #{packet}, #{actionType})
    </insert>

    <!-- 기존 패턴 로그를 업데이트합니다 -->
    <update id="update"
            parameterType="com.example.demo.dto.log.PatternLog">
        UPDATE log_db.pattern_log_20240719
        SET time = #{time}, s_ip = #{sIp}, d_ip = #{dIp}, s_port = #{sPort},
            d_port = #{dPort}, len = #{len}, pattern1 = #{pattern1}, pattern2 =
            #{pattern2}, pattern3 = #{pattern3}, packet = #{packet}, action_type =
            #{actionType}
        WHERE id = #{id}
    </update>

    <!-- ID를 기반으로 특정 패턴 로그를 삭제합니다 -->
    <delete id="delete" parameterType="int">
        DELETE FROM log_db.pattern_log_20240719 WHERE id = #{id}
    </delete>

    <!-- Top 5 s_ip 값 조회 -->
    <select id="findTopSIPs" resultType="java.util.Map">
        SELECT s_ip, COUNT(*) AS ip_count
        FROM log_db.pattern_log_20240719
        GROUP BY s_ip
        ORDER BY ip_count DESC
        LIMIT 5;
    </select>

    <!-- Top 3 패턴 조합 조회 -->
    <select id="findTopPatternCombinations"
            resultType="java.util.Map">
        SELECT CONCAT(pattern1, ',', pattern2, ',', pattern3) AS combined_pattern,
               COUNT(*) AS pattern_count
        FROM log_db.pattern_log_20240719
        GROUP BY combined_pattern
        ORDER BY pattern_count DESC
        LIMIT 3;
    </select>

    <!-- 모든 패턴 로그를 최신 순으로 조회 -->
    <select id="findAllOrderedByTimeDesc"
            resultMap="patternLogResultMap">
        SELECT *
        FROM log_db.pattern_log_20240719
        ORDER BY time DESC;
    </select>

    <!-- 다중 검색 기능을 위한 패턴 로그 조회 -->
<select id="findByMultipleCriteria" resultMap="patternLogResultMap" parameterType="map">
    SELECT *
    FROM log_db.pattern_log_20240719
    WHERE 1=1
    <!-- ID 검색 조건 -->
    <if test="id != null">
        AND id LIKE CONCAT('%', #{id}, '%')
    </if>
    <!-- 시간 검색 조건 -->
    <if test="time != null">
        AND time LIKE CONCAT('%', #{time}, '%')
    </if>
    <!-- 송신 IP 검색 조건 -->
    <if test="sIp != null">
        AND s_ip LIKE CONCAT('%', #{sIp}, '%')
    </if>
    <!-- 수신 IP 검색 조건 -->
    <if test="dIp != null">
        AND d_ip LIKE CONCAT('%', #{dIp}, '%')
    </if>
    <!-- 송신 포트 검색 조건 -->
    <if test="sPort != null">
        AND s_port LIKE CONCAT('%', #{sPort}, '%')
    </if>
    <!-- 수신 포트 검색 조건 -->
    <if test="dPort != null">
        AND d_portLIKE CONCAT('%', #{dPort}, '%')
    </if>
    <!-- 길이 검색 조건 -->
    <if test="len != null">
        AND len =LIKE CONCAT('%', #{len}, '%')
    </if>
    <!-- 패턴 1 검색 조건 -->
    <if test="pattern1 != null">
        AND pattern1 LIKE CONCAT('%', #{pattern1}, '%')
    </if>
    <!-- 패턴 2 검색 조건 -->
    <if test="pattern2 != null">
        AND pattern2 LIKE CONCAT('%', #{pattern2}, '%')
    </if>
    <!-- 패턴 3 검색 조건 -->
    <if test="pattern3 != null">
        AND pattern3 LIKE CONCAT('%', #{pattern3}, '%')
    </if>
    <!-- 액션 타입 검색 조건 -->
    <if test="actionType != null">
        AND action_typeLIKE CONCAT('%', #{actionType}, '%')
    </if>
  
</select>


</mapper>
