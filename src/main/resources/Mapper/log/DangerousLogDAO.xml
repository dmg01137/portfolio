<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.dao.log.DangerousLogDAO">

    <!-- Select by ID -->
    <select id="findBydangerous_log" resultType="com.example.demo.dto.log.DangerousLog" parameterType="int">
        SELECT * FROM dangerous_log WHERE dangerous_number = #{dangerous_number}
    </select>

    <!-- Select all -->
    <select id="findAll" resultType="com.example.demo.dto.log.DangerousLog">
        SELECT * FROM log_db.dangerous_log_20240718
    </select>

    <!-- Insert -->
    <insert id="save" parameterType="com.example.demo.dto.log.DangerousLog">
        INSERT INTO log_db.dangerous_log (ip, port,detection_number)
        VALUES (#{ip}, #{port}, #{detection_number})
    </insert>

    <!-- Update -->
    <update id="update" parameterType="com.example.demo.dto.log.DangerousLog">
        UPDATE log_db.dangerous_log
        SET ip = #{ip}, port = #{port}, detection_number = #{detection_number}
        WHERE dangerous_number = #{dangerous_number}
    </update>

    <!-- Delete -->
    <delete id="delete" parameterType="int">
        DELETE FROM dangerous_log WHERE dangerous_number = #{dangerous_number}
    </delete>

    <!-- Search -->
    <select id="search" resultType="com.example.demo.dto.log.DangerousLog">
        SELECT * FROM log_db.dangerous_log
        WHERE 
        <if test="dangerous_number != null">
            dangerous_number = #{dangerous_number}
        </if>
        <if test="ip != null and ip != ''">
            AND ip = #{ip}
        </if>
        <if test="port != null">
            AND port = #{port}
        </if>
        <if test="detection_number != null and detection_number != ''">
            AND detection_number = #{detection_number}
        </if>
    </select>

</mapper>
